<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verification</title>
    <style>
    body {
        background: linear-gradient(120deg, #3a7bd5 0%, #3a6073 100%);
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: Arial, sans-serif;
    }

    .verification-container {
        width: 90%;
        max-width: 350px;
        padding: 16px;
        background-color: rgba(0, 0, 0, 0.7);
        color: #D6BD98;
        border-radius: 8px;
        box-shadow: 0px 0px 10px 0px rgba(0,0,0,0.1);
    }

    .verification-container h1 {
        text-align: center;
    }

    .verification-container input[type="number"] {
        width: 100%;
        padding: 12px 20px;
        margin: 8px 0;
        display: inline-block;
        border: 1px solid #ccc;
        box-sizing: border-box;
        transition: border 0.3s ease;
    }

    .verification-container input[type="number"]:focus {
        border-color: #3a7bd5;
    }

    .verification-container button {
        background: linear-gradient(45deg, #3a7bd5, #3a6073);
        color: white;
        padding: 14px 20px;
        margin: 8px 0;
        border: none;
        cursor: pointer;
        width: 100%;
        transition: background 0.3s ease;
    }

    .verification-container button:hover {
        background: linear-gradient(45deg, #3a6073, #3a7bd5);
    }

    h1 {
        color: #D6BD98;
        padding-bottom: 10px;
    }
    </style>
    <script>
        window.onload = function() {
            alert("Your verification code has been sent to your registered email ID.");
        };
    </script>
</head>
<body>
    <div class="verification-container">
        <form action="/Principal/verify" method="post">
            <h1>Verification Code</h1>
          
            <input type="hidden" name="username" value= <%= username %>>

            <label for="verification-code"><b>Enter Verification Code:</b></label>
            <input type="number" placeholder="Enter Code" name="otp" required>

            <button type="submit">Verify</button>
        </form>
    </div>
</body>
</html>
