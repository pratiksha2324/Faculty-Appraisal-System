<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SuperAdmin Dashboard</title>
    <style>
        body {
            margin: 0; /* Ensure no margin on the body */
            background: linear-gradient(120deg, #3a7bd5 0%, #3a6073 100%);
            height: 100vh;
            display: flex;
            flex-direction: column; /* Set body to column to stack nav and container */
            font-family: Arial, sans-serif;
        }

        .container {
            margin: auto; /* Centers the container */
            width: 95%;
            height: 720px;
            margin: 10px;;
            padding: 16px;
            background-color: rgba(0, 0, 0, 0.7);
            color: #D6BD98;
            border-radius: 8px;
            box-shadow: 0px 0px 10px 0px rgba(0,0,0,0.1);
        }

        .container a {
            color: #D6BD98;
            text-decoration: none;
            margin-right: 15px;
        }

        .container a:hover {
            text-decoration: underline;
        }

        h1 {
            color: #D6BD98;
            padding-bottom: 10px;
            text-align: center;
        }

        nav {
            position: sticky; /* Keep nav at the top */
            top: 0;
            z-index: 1000; /* Ensures nav is above other content */
        }
        .welcome-message {
            margin: 20px; /* Adds some margin for spacing */
            color: #D6BD98;
            text-align: left; /* Ensures text is aligned to the left */
        }

    </style>
</head>
<body>
    <!-- Include nav.ejs at the top of the body -->
    <%- include("nav")  %>
    <div class="container">
        <h1 class="welcome-message">Welcome Apurva !!!!!</h1>
       
    </div>
</body>
</html>
