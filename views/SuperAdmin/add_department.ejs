<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Department</title>
    <style>
        body {
            margin: 0;
            background: linear-gradient(120deg, #3a7bd5 0%, #3a6073 100%);
            height: 100vh;
            
        }
.main{
    display: flex;
            flex-direction: column;
            font-family: Arial, sans-serif;
            align-items: center;
            justify-content: center;
            margin-top: 30px;

}
        .container {
            width: 90%;
            max-width: 600px;
            padding: 20px;
            background-color: rgba(0, 0, 0, 0.7);
            color: #D6BD98;
            border-radius: 8px;
            box-shadow: 0px 0px 10px 0px rgba(0, 0, 0, 0.1);
            box-sizing: border-box;
        }

        h1 {
            color: #D6BD98;
            padding-bottom: 10px;
            text-align: center;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            margin-bottom: 8px;
            font-weight: bold;
            color: #D6BD98;
        }

        input[type="text"],
        select {
            width: 80%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 4px;
            background-color: #fff;
            color: #333;
            font-size: 16px;
        }

        button {
            padding: 10px;
            background-color: #28a745;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            margin-top: 10px;
            transition: background-color 0.3s;
            width: 400px;
        }

        button:hover {
            background-color: #218838;
        }

        nav {
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        @media (max-width: 600px) {
            .container {
                width: 100%;
                margin: 0 10px;
            }

            
        }
    </style>
</head>
<body>
    <!-- Include nav.ejs at the top of the body -->
    <%- include("nav") %>
    <div class="main">
    <div class="container">
        <h1>Add Department</h1>
        <form action="/add-department" method="POST">
            <label for="institute_id">Select Institute:</label>
            <select id="institute_id" name="institute_id" required>
                <option value="">Select an institute</option>
                <!-- Replace the following options with dynamic options from your server/database -->
                <option value="1">Institute A</option>
                <option value="2">Institute B</option>
                <option value="3">Institute C</option>
            </select>

            <label for="department_name">Department Name:</label>
            <input type="text" id="department_name" name="department_name" required>

            <button type="submit">Add Department</button>
        </form>
    </div>
</div>
</body>
</html>
